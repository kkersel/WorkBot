# ============ Secrets / local config ============
.env
.env.local
.env.*.local
*.env

# ============ Python (bot) ============
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
develop-eggs/
downloads/
eggs/
.eggs/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.venv/
venv/
env/
ENV/
# NOTE: do NOT ignore generic `lib/`, `build/`, `dist/` at repo root —
# they collide with webapp/ source dirs. Python venv stuff is already
# excluded via the .venv/venv/env/ENV entries above.

# ============ Node / Next.js (webapp) ============
node_modules/
.next/
.turbo/
.vercel/
out/
webapp/dist/
*.tsbuildinfo
next-env.d.ts
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# ============ Editors / OS ============
.DS_Store
.idea/
.vscode/
*.swp
*.swo
Thumbs.db

# ============ Logs ============
*.log
logs/

# ============ Local data (old bot — keep for migration but don't recommit) ============
# data.json   # старая БД — оставляем до миграции

# ============ Test / coverage ============
.coverage
htmlcov/
.pytest_cache/
.mypy_cache/
.ruff_cache/
